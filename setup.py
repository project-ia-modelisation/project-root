from setuptools import setup, find_packages

setup(
    name="project_root",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Ajoute tes dépendances ici (ex: torch, flask...)
    ],
)
